# Copy this file to `.env` in the project root and fill in your actual credentials.
# Example for a local PostgreSQL running on default port with a database called university_db.
PGHOST=127.0.0.1
PGPORT=5433
PGDATABASE=university_db
PGUSER=postgres
PGPASSWORD=6667
